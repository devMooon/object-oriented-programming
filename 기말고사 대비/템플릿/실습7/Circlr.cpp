#include "Circlr.h"
#include "Comparable.h"
bool Circlr::operator<(const Comparable& comparable) const
{
	//if (this->radius < comparable.radius) return true;
	else return false;
}
bool Circlr::operator>(const Comparable& comparable) const
{

}